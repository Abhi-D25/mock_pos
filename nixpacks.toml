# Nixpacks configuration for Railway
# This ensures proper Node.js version and build steps

[phases.setup]
nixPkgs = ["nodejs-18_x", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run railway:start"
